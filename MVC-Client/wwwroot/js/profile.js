window.onload = function () {
    if (window.jQuery) {
        // jQuery is loaded  
        console.log(window.jQuery)
     
    } else {
        // jQuery is not loaded
        alert("Doesn't Work");
    }
}